fun main() {
    val valKeyword = "val"
    val varKeyword = "var"
    val canChange = "can change"
    val cantChange = "doesn't change"
    println("Use the $valKeyword keyword when the value $cantChange.")
    println("Use the $varKeyword keyword when the value $canChange.")
    println("When you define a function, you define the parameters that can be passed to it.")
    println("When you call a function, you pass arguments for the parameters.")
}